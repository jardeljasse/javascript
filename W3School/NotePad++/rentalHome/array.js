const numbers = [45, 4, 9, 16, 25];
let txt = "";
function myFunction(value) {
    txt += value + "<br>";
}

let elTipoDeCasa = document.querySelector('.tipo').innerHTML = document.write(numbers.forEach(myFunction));